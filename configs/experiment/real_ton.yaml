# @package _global_
defaults:
  - override /dataset: ton_iot
  - override /partition: source_k
  - override /aggregator: fedavg
  - override /scorer: global_val_harm
  - _self_

experiment:
  name: real_ton
  notes: "Real-source FL on ToN-IoT (source-id clients, no label/update poisoning)."
